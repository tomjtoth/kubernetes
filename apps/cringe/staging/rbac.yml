apiVersion: v1
kind: ServiceAccount
metadata:
  name: staging-cringe-hook
  namespace: staging-cringe

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: staging-cringe-hook

rules:
  - apiGroups:
      -
    resources:
      - pods
    verbs:
      - get
      - list
      - watch

  - apiGroups:
      -
    resources:
      - persistentvolumeclaims
      - persistentvolumes
    verbs:
      - get
      - list
      - create
      - delete
      - patch
      - watch

  - apiGroups:
      - apps
    resources:
      - deployments
    verbs:
      - get
      - list
      - patch
      - watch

  - apiGroups:
      - longhorn.io
    resources:
      - backups
      - volumes
      - backupvolumes
    verbs:
      - get
      - list
      - create
      - delete

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: staging-cringe-hook

subjects:
  - kind: ServiceAccount
    name: staging-cringe-hook
    namespace: staging-cringe
roleRef:
  kind: ClusterRole
  name: staging-cringe-hook
  apiGroup: rbac.authorization.k8s.io
